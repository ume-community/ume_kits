name: cyclop
description: An experimental flutter colorpicker with an optional eyedropper tool ( Material colors swatches, AHSL, ARGB and custom swatches library )
version: 0.5.2
publish_to: none
# homepage: https://github.com/rxlabz/cyclop

environment:
  #[Original] sdk: ">=2.12.0 <3.0.0"
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.0.15
  quiver: ^3.2.1
  basics: ^0.10.0
  # ume_kit_ui:
  # path: ../../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
