name: ume_kit_example
description: A example project for developer to develop their own ume kit.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ume: ^2.0.0

  cupertino_icons: ^1.0.0

dependenices_overrides:
  shared_preferences: 0.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
